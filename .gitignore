.env
__pycache__/
*.pyc
.venv/
venv/
reports/

# metrics_history.csv と metrics_graph.png は永続的な指標の蓄積として
# リポジトリで追跡する（GitHub Actions が毎日更新してコミットする）。

# VC在室スナップショットの履歴は専用のデータブランチ（vc-log）で管理するため、
# main では追跡しない。ローカル実行やCIの worktree 展開先も対象。
vc_history.csv
vc-log/
